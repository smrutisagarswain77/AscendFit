# ── PostgreSQL (Docker) ───────────────────────────────────────
POSTGRES_DB=ascendfit
POSTGRES_USER=ascendfit
POSTGRES_PASSWORD=ascendfit
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Connection string for Django (Step 2+)
DATABASE_URL=postgres://ascendfit:ascendfit@localhost:5432/ascendfit

# ── Backend (Step 2+) ─────────────────────────────────────────
# DJANGO_SETTINGS_MODULE=ascendfit.settings.development
# SECRET_KEY=change-me-in-production
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2
# CORS_ALLOWED_ORIGINS=http://localhost:8081,http://10.0.2.2:8081
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# ── Mobile (Step 6+) ──────────────────────────────────────────
# API_BASE_URL=http://10.0.2.2:8000/api/
